services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      REDIS_URL: "redis://redis:6379/0"
      SHARE_TTL_HOURS: "168"
      MAX_SHARE_TTL_HOURS: "720"
      MAX_BODY_BYTES: "2097152"
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
